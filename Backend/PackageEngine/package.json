{
  "name": "iumrah-beta-package-engine",
  "private": true,
  "version": "1.0.0-clean",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.mjs && bash scripts/runtime-tests.sh",
    "deploy": "wrangler deploy --config wrangler.generated.jsonc"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.107.0"
  }
}
