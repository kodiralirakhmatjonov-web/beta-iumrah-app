import SwiftUI

// Retained as a compatibility source file for the current XcodeGen project.
// The drawer navigation was removed in favor of the permanent Account tab.
struct SidebarDrawerView: View {
    var body: some View {
        EmptyView()
    }
}
